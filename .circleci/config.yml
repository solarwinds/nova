version: 2.1
commands:
    bits-artifact-number:
        parameters:
            value:
                default: "latest"
                type: string
        steps:
            - run: echo "Hello <<parameters.value>>"
jobs:
    bits-build:
        working_directory: ~/nova
        docker:
            -   image: circleci/node:12-browsers
        steps:
            -   checkout:
                    path: ~/nova
            -   run:
                    working_directory: ~/nova/packages/bits
                    name: Install dependencies
                    command: yarn install
            -   run:
                    working_directory: ~/nova/packages/bits
                    name: Assemble
                    command: yarn run assemble
            -   persist_to_workspace:
                    root: .
                    paths:
                        - .
    bits-unit-test:
        working_directory: ~/nova/packages/bits
        docker:
            -   image: circleci/node:12-browsers
        steps:
            -   attach_workspace:
                    at: ~/nova
            -   run:
                    name: Unit tests
                    command: yarn run test
    bits-e2e-test:
        working_directory: ~/nova
        machine:
            image: ubuntu-2004:202010-01
        environment:
            PROJECT_DIR: ~/nova/packages/bits
        steps:
            -   attach_workspace:
                    at: ~/nova
            -   run:
                    command: pwd; ls
            -   run:
                    name: Start Selenium docker image
                    command: docker-compose up -d --build selenium
            -   run:
                    name: E2E tests
                    command: docker-compose run --user="$UID" build npm run e2e:ci --prefix ./packages/bits
            -   run:
                    name: Cleanup
                    command: docker-compose down -v --rmi="all"
    bits-visual-test:
        working_directory: ~/nova
        machine:
            image: ubuntu-2004:202010-01
        environment:
            PROJECT_DIR: ~/nova/packages/bits
        steps:
            -   attach_workspace:
                    at: ~/nova
            -   run:
                    command: pwd; ls
            -   run:
                    name: Start Selenium docker image
                    command: docker-compose up -d --build selenium
            -   run:
                    name: Visual tests
                    command: docker-compose run --user="$UID" build npm run visual:ci --prefix ./packages/bits
            -   run:
                    name: Cleanup
                    command: docker-compose down -v --rmi="all"
    bits-publish:
        working_directory: ~/nova/packages/bits
        docker:
            -   image: circleci/node:12-browsers
        steps:
            -   attach_workspace:
                    at: ~/nova
            -   run:
                    name: Create bits package
                    command: npm pack ./dist
            -   bits-artifact-number:
                    value: "${CIRCLE_BUILD_NUM}"
    charts-build:
        working_directory: ~/nova
        docker:
            -   image: circleci/node:12-browsers
        steps:
            -   checkout:
                    path: ~/nova
            -   run:
                    name: Download Bits Artifact
                    # TODO: replace "207" with correct build number
#                    command: |
#                        curl -H "Circle-Token: ${CIRCLE_API_TOKEN}" https://circleci.com/api/v1.1/project/github/${CIRCLE_PROJECT_USERNAME}/${CIRLCE_PROJECT_REPONAME}/207/artifacts \
#                        | grep -o 'https://[^"]*' \
#                        | wget --verbose --header "Circle-Token: ${CIRCLE_API_TOKEN}" --input-file -
                    command: echo ${CIRCLE_API_TOKEN}
            -   run:
                    name: without {}
                    command: echo $CIRCLE_API_TOKEN
#            -   run:
#                    working_directory: ~/nova/packages/charts
#                    name: Install Bits
#                    command: yarn add ~/nova/bits-package.tgz
#            -   run:
#                    working_directory: ~/nova/packages/charts
#                    name: Install dependencies
#                    command: yarn install
#            -   run:
#                    working_directory: ~/nova/packages/charts
#                    name: Assemble
#                    command: yarn run assemble
            -   persist_to_workspace:
                    root: .
                    paths:
                        - .

workflows:
    version: 2
    nova:
        jobs:
#            -   bits-build
            # - bits-unit-test:
            #     requires:
            #         - bits-build
            # - bits-e2e-test:
            #     requires:
            #         - bits-build
#            -   bits-publish:
#                    requires:
#                        - bits-build # TODO: remove
                        # - bits-unit-test
                        # - bits-e2e-test
            -   charts-build
#                    requires:
#                        - bits-publish
