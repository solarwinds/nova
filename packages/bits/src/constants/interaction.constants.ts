/**
 * These selectors are useful when we capture e.g. a click event on a higher level and want to prevent an action in case the event target was one of these.
 */
export const DEFAULT_INTERACTIVE_ELEMENTS = ["button", "input[type='button']", "a[href]"];
